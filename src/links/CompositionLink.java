package links;

import objects.BaseObject;

public class CompositionLink extends BaseLink {
    public CompositionLink(BaseObject origin, BaseObject destination) {
        super(origin, destination);
    }
}
