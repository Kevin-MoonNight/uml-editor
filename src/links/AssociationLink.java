package links;

import objects.BaseObject;

public class AssociationLink extends BaseLink {
    public AssociationLink(BaseObject origin, BaseObject destination) {
        super(origin, destination);
    }
}
